version: '3.1'

services:
  redis-isolated:
    image: redis:6.2-alpine
    restart: always
    ports:
      - 6379:6379
    command: redis-server --save 20 1 --loglevel warning --requirepass ${COMMON_PASSWORD}
    volumes:
      - redisisolateddata:/data

volumes:
  redisisolated:

networks:
  default:
    name: compose-shared
    external: true
